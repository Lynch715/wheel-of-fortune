# 施工记录 v3.7 · 换脸

> 对应《规范 v3.7 换脸》，第 5 节按**乙案**：既能从图集里挑，也能上传自己的图。
> 施工日：2026-09-23。`index.html` 2,540,848 → **2,552,129** 字节。
> 改前的整页备份在 `归档/index_v3.6_换脸前.html`。
> 自测：新开 `test/s21.js` 39 项；s1–s20、手机 54 项、跑量 60 局 660 回合，全部通过。
> 存档版本不动。新加的两个字段 `avatarPick`、`face`，旧存档里没有就当没换过。

---

## 一、入口

- **主角**：点左上角面板上自己的头像。
- **NPC**：人物卡头像旁边的身份那几行底下，多了一个小按钮「换脸」。人死了也能换。
- 主界面没有加按钮。比武框、对话框里的头像会跟着换，这两处没有入口。

## 二、换脸框

- 分六页：东荒、西陆、樱洲、轮枢、特型、自己的图。图集里 90 张全部能挑，不限界，也不限年龄段。
- 打开时停在这个人当前那张脸所在的一页（没换过脸的就是他所在的界），性别先筛成他自己的；右上角「男女／男／女」可以切换。特型页和自己的图那页不筛性别。
- 点一格，下面显示这张脸的相貌描述；点「用这张」才算换好。「随天意」是回到自动抽的那张。
- 以后图集加了格子，这里会自动多出来。

## 三、换了以后

| | 从图集挑 | 自己的图 | 随天意 |
|---|---|---|---|
| 存什么 | `n.avatar` = 那一格，`n.avatarPick = true` | `n.face = 'u_…'` | 两个记号都清掉，按原规则重算 |
| 相貌文字 | `portrait` 换成这一格的描述 | 换成你写的那一句；没写就不动 | 换回自动那一张的描述 |
| 立绘 | 那一格有立绘就显示 | 不显示 | 同左 |

- **手选的脸优先于所有自动规则**：长了岁数不会换，幽墟头目也不会被 `BOSS_AV` 顶回原来那张。头目点「随天意」后，会回到 `BOSS_AV` 指定的那张。
- 主角换脸时，`appearance` 会跟着改。这一栏是面板上「相貌」显示的内容，也是模型写主角时看的。NPC 的 `appearance` 本来只是补充伤疤、神态这类细节，所以没动，只换了 `portrait`。
- 下一回合起模型就按新脸写。换脸不花模型调用，也没有记进台账：台账会进提示词里的「已成定局」那一栏，一句「换了张脸」混进去只会让模型莫名其妙。规范里原本写了「起居注记一行」，这一条没做。

## 四、自己的图

- 换脸框「自己的图」那一页，第一格是「＋」，点了从电脑或手机里挑一张图。
- 图裁成正方形。竖图从偏上的位置裁，因为脸一般在上半截。然后压成 160×160，优先存 webp，老 Safari 压不了 webp 就改成 jpeg；一张大约几 KB 到十几 KB。
- 图存在浏览器的 IndexedDB 里（跟章节库放在同一个库，库版本从 1 升到 2，加了一个 `faces` 表），**不放进存档那一格**，所以存档不会被撑大。
- 选中自己的图时，下面会出一栏「这张图的相貌」，写一句话，模型就照这句写人。这句话记在图上，下次别的人用这张图也会带上。
- 「删掉这张图」会先确认，提示里会写出谁正在用它；删掉后，用它的人换回原来的脸。
- 图没读到时（比如换了设备、清了浏览器数据），头像退回到图集那一张，`n.face` 保留不删，以后图回来了又能显示。

**导出和导入**：「导出存档」时，会把用到的那几张图一起写进 json 的 `_faces` 字段；「导入存档」时先把图放回 IndexedDB，再读档，`_faces` 不会进存档那一格。在新设备上导入，头像照样是那张，刷新后也还在。

存档阁里的三个存档位走的是 localStorage，不带图。图在同一个浏览器的 IndexedDB 里，读档时照样找得到。

## 五、测试

`test/s21.js` 共 39 项，覆盖：

- **主角**：点头像能打开换脸框；默认页和性别筛选对；换完后头像、相貌文字、面板都跟着变。
- **NPC**：人物卡的换脸入口；特型页有立绘的会显示立绘；手选以后长了岁数也不变；下一回合的提示词用的是新相貌。
- **头目**：手选不会被 `BOSS_AV` 顶回去；「随天意」能回到 `BOSS_AV` 那张。
- **自己的图**：上传后裁成 160、体积很小；存档那一格不带图，图进了 IndexedDB；导出时带上图，换一个全新浏览器导入后图和头像都回来，刷新也还在；图丢了退回图集那张；删图后用它的人换回原来的脸。
- **其他**：老存档读进来脸不变；中间档也能换；手机 390 宽下不横向溢出，格子至少 44px。

回归里 s9 有一项「钱不够就不走」失败过一次，单独重跑三次全都通过。这一项跟换脸无关，是它原本就有的随机波动，记在这里。

还是没推 GitHub。
