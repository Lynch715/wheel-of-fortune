# 命运之轮 · 出图作业单 v4.0「批量阶段」

> 2026-09-20 定的节奏。配套《美术规范_v4.0_人物形象.md》与三份出图清单。
> 已封版：S1 三张（`h2m_01`、`d3f_01`、`w4m_02`）、`d1f` 四张、`d1m` 四张。**待出 239 张。**
>
> **2026-09-20 减量**：桶从每桶 4 张退到 **3 张**（`_04` 转备选），捏人 146 → 116，总量 280 → 250。捏人页每界每性别仍有 12 张可选，比旧版的 8 张多。已出满 4 张的 `d1f`、`d1m` 保留。

## 0. 节奏

硬指标（尺寸、底色、留白）**不再逐桶确认**，全部后置，最后统一跑脚本。
内容风险（年龄漂、撞脸）集中在少年和青年两档，只在这两档各设一个样张桶。

```
① s1m 样张桶（3 张）──确认──┐
② w2f 样张桶（3 张）──确认──┴→ ③ 其余 233 张一次性全出 → ④ 统一规格化 → ⑤ 总验收
```

中年、老年两档不设样张：`d3f_01` 和 `w4m_02` 已经证明这两档画得准，直接进 ③。

---

## 1. 两个样张桶

| 样张 | 是什么 | 为什么选它 |
|---|---|---|
| `s1m` | 樱洲少年男 3 张 | 少年档的年龄锚点是在东荒女（`d1f`）上调出来的，换一个界、换一个性别，验证它是不是通用 |
| `w2f` | 西陆青年女 3 张 | 再换一界一性别。青年档（18–35）最容易被画成三十五岁往上 |

两桶都过了，年龄锚点就算验证完毕，后面不再逐桶确认。
有一桶没过，只修那一桶，修完再往下——**不要在样张没过的情况下开批量**。

---

## 2. 每张图必须带的四样东西

1. **总咒语**（规范第 3 节整段，一字不改）
2. **年龄锚点**（规范 9.2，少年、青年两档必带；中老年档按条目里的年岁写）
3. **参考图两张**：
   - 画风参考 → `avatars/d3f_01.png`
   - 年龄参考 → 少年档传 `d1f_01`～`d1f_04` 任一张；青年档传 `h2m_01.png`
   - 传参考图时必须跟上规范 2.3 那句：**参考图只对齐画风，长相必须不是同一个人**
4. **本张的九项**（骨相／眉眼／鼻口／肤与痕／体格／神气／服制／标记／机位），照清单抄

---

## 3. 批量阶段的分批与顺序

### 捏人 105 张

| 批 | 内容 | 张数 |
|---|---|---|
| B1 | 少年档剩余 5 桶：`w1m` `w1f` `s1f` `h1m` `h1f`（`d1m` 已封版） | 15 |
| B2 | 青年档剩余 6 桶：`d2m` `d2f` `w2m` `s2m` `s2f` `h2f`，以及 `h2m_02` `h2m_03` | 20 |
| B3 | 中年档 7 桶：`d3m` `w3m` `w3f` `s3m` `s3f` `h3m` `h3f`，以及 `d3f_02` `d3f_03` | 23 |
| B4 | 老年档 7 桶：`d4m` `d4f` `w4f` `s4m` `s4f` `h4m` `h4f`，以及 `w4m_01` `w4m_03` | 23 |
| B5 | 特型 18 张 `x_*` | 18 |

> `h2m`、`d3f`、`w4m` 三个桶各已有一张（S1 转正的），只补另外两张，**新补的要跟已有那张拉开骨相、体格、肤色**。

### 名人 112 张 + 头目 22 张

**按互斥矩阵分组出，同组连着出**，这样出完立刻能并排比。

| 批 | 内容 | 张数 |
|---|---|---|
| B6 | 东荒：三清与至高 5 → 天庭 6 → 佛门 8 → 玉清上清散人 9 → 战将妖王 9 → 地府 3 → 人间儒道 5 → 破格 T4 8 | 53 |
| B7 | 西陆 29 张：至高传奇 5 → 圆桌骑士 8 → 龙 5 → 精灵矮人学者 11 | 29 |
| B8 | 樱洲 25 张：至高与阴阳师 5 → 妖 3 → 剑客武将 9 → 忍者 4 → 战国 5（伊达政宗是第 26 张，待定） | 25 |
| B9 | 幽墟非头目 5 + 头目 22（按层出，第 6 层的吞噬者最后） | 27 |

---

## 4. 收尾：统一规格化与总验收

### 第一步 规格化

```
python3 tools/normalize.py <原图目录> <输出目录>
```

三个目录各跑一遍：`avatars`、`fame`、`bosses`。尺寸、底色、头顶留白这三条在这里一次性解决，出图阶段不必管。

### 第二步 总验收

```
python3 tools/check_all.py art/万界v4
```

产出：

- `_验收报告.md` —— 每个目录的张数、分组数、**硬指标不合格清单**
- `_检查_<桶>.png` —— 32 个桶各一张，上排全图、下排只留脸
- `_总览.png` —— 全部 250 张的联系表，一屏扫画风有没有飘

### 第三步 人眼三关（脚本替不了）

1. **遮发遮衣互斥**：看每个 `_检查_<桶>.png` 的下排，分不出是几个人的重出
2. **年龄同档**：一个桶里不能一张十五岁一张三十岁
3. **名人互斥矩阵**：规范第 4 节那十组并排检查；头目另做 **64px 小圆测试**（缩到 64px 还分得出谁是谁）

### 第四步 只重画不合格的单张

不合格的挑出来单独重出，重出后再跑一次 `normalize.py` + `check_all.py`。**不要整桶重来。**

---

## 5. 几条容易踩的

- 一个桶里别放两张同类脸（`d1f` 的 01、02 都是「暖肤圆脸在笑」，已是极限）
- 景别以 `d3f_01` 为准：头顶到下巴约占画面 30%，肩线压在底部三分之一
- 西陆的桶别画成四张白人面孔，可有南方深肤、北地苍白、东方商队后裔
- 轮枢的桶混血感是特色，五官和肤色可以随便混
- 幽墟那 22 张统一压低明度，但**背景仍是 `#E4E0D6`**，不许加黑雾光晕——圆裁后会留脏边
- 名人不画法器，标记长在身上（眉心痕、竖瞳、鳞、角、白毫、疤、异色瞳、发色）

---

## 6. 武侠江湖模拟器那批资产怎么用

`武侠江湖模拟器/avatars/` 有 138 张，其中 **82 张就是现在 `art/万界/avatars` 那套**（S4 当初从那儿复制过来的，正是这次要淘汰的），真正独有的 56 张是 `m1–m4`、`f1–f4` 各 6 张加 8 张特型。

**不直接混进 v4 图集。** 那批是水墨淡彩路子，线条感强、皮肤没质感，底色暖黄且每张不一样（`#F4E3C2`、`#E9DBC4`、`#F0E2C8`），512×512；`f1`、`f2` 那几张还是大眼尖下巴的动漫美少女——正是 v4 要摆脱的东西。混在一张图集里，捏人页一屏就能看出是两拨人画的。

**能用的地方**：出 `d*` 桶时把武侠同年龄段的图一起传当**人设参考**（不是画风参考），省想人设的工夫，画风仍按 v4 的总咒语走。

省量靠的是上面那条「每桶 3 张」，不是靠混用旧图。
