# 规范 v3.8 · 括号里的推演要求

> 2026-09-23。要求：随心所欲档下，玩家行动里用括号「（）」写的描写或推演要求，模型要无条件照办。
> 先看规范，确认再动工。第 4 节有两项要你拍板。

---

## 1. 现在是什么情况

玩家输入原样塞进【玩家本回合行动】那一行，括号里的字跟行动混在一起。模型把它当成行动的一部分来读，并不知道这是写给它看的要求，所以：

- 「我拔剑（写得慢一点，把他的表情写细）」：模型多半只写拔剑，括号里的要求当没看见；
- 「我去酒馆（这回合写两千字）」：会被「700–1000 字」那条压回去；
- 「我走进山门（让掌门正好在门口等我）」：v3.6 以后能成，但这是托【言出法随】的福，不是因为模型认得括号。

另外括号里的字还会被引擎当成行动去扫：「（写他想去西陆的心思）」会被 `travelIntent()` 当成要去西陆；`guessAttr()` 挑属性时也会把括号里的字算进去。

## 2. 做法

### 2.1 引擎先把括号拆出来

新函数 `splitAsk(text)`：认全角「（）」和半角「()」两种，嵌套的按最外层算。拆成两半：

- `act`：去掉括号以后的行动本身；
- `asks`：括号里的每一段，一条一条列出来。

**只在随心所欲档拆。** 另外两档原样不动，括号照旧跟着行动一起走，跟现在一模一样。

拆完以后，过界判断（`travelIntent`）、挑属性（`guessAttr`）、阿步暗号都只看 `act`，括号里的字不再被当成行动。

### 2.2 进提示词

【玩家本回合行动】只写 `act`，紧跟着多一栏：

```
【玩家本回合行动】我走进酒馆
【玩家对这一回合的推演要求（逐条照办）】
1. 写得慢一点，把掌柜的表情写细
2. 让那个穿灰袍的人正好坐在角落
3. 这一回合写两千字
```

这一栏跟行动一样每回合都会变，所以放在后半截，不碰缓存前缀。

【言出法随】里加一段（这一段每回合都一样，进缓存前缀）：

```
- 玩家在行动后面用括号写的，是他对这一回合描写和推演的要求，会单独列在【玩家对这一回合的推演要求】里。
  逐条照办、写足，不许省略、打折、换成别的，也不许拿「为了剧情合理」当理由改掉。
  这些要求压过【写作要求】里的字数、段落、对话处数、衔接、「必须往前一步」这些条款：他要写两千字就写两千字，要停在这一刻不往前推就停住，要只写心理活动就只写心理活动。
  只有两样不让：输出的 JSON 格式照旧（该有的字段都要有）；引擎掷出来的结果照旧（见下面第 4 节）。
```

### 2.3 界面

章节开头那条行动回显「➤ 我走进酒馆（写得慢一点……）」照原样显示，括号那段用浅一号的字，一眼能看出哪些是要求、哪些是行动。

## 3. 面谈里也认括号

面谈时你打的话也常带括号，比如「（拍拍他的肩）你先别急」「（我想听他讲当年的事，讲细一点）」。这里的括号有两种意思：一种是动作，一种是要求，混着用。

做法是拆出来以后都告诉模型：「括号里是主角的动作或者对这一段的要求，动作照着写进场面，要求照办」。模型分得清这两种，不用引擎去猜。

同样只在随心所欲档这么做。

## 4. 要你拍板

**① 括号能不能越过引擎的账？**

v3.6 定的甲案是「NPC 的态度全放开，数值还是引擎说了算」。括号是更直接的口子：「（写我拿到了一万两）」「（让我修为直接突破到金丹）」「（这一回合过去三年）」。

| | 做法 | 结果 |
|---|---|---|
| **甲（推荐）** | 描写、剧情走向、谁出场、谁怎么反应、写多长、写成什么调子，全听括号的。**银钱、修为、寿元、时间这些由引擎记账的数照旧**：剧情可以写你捡到一万两，账上进多少还是引擎按规矩算；要跳三年，就写成「一晃三年」，但日子还是按引擎给的天数走 | 跟 v3.6 一个口径，不会一句话通关 |
| 乙 | 括号里点名要的数也照给：模型在 playerChanges 里写多少，引擎就收多少，不再卡上限 | 真正的心想事成，代价是界主、万界榜、轮外之物这几条线都能一句话打穿 |

**② 世界的两条边界要不要也让括号越过？**

【言出法随】现在还留着两条不让：这个世界里没有的东西不给，已经成了定局的事不能推翻（死人不能复活、终身旧伤不能好）。

- **守住（推荐）**：括号也越不过这两条，模型会写成「这件事本身做不到」。不然一句「（让师父活过来）」就把前面的仇、宿命、台账全架空了。
- **放开**：括号里的话压过一切，死人也能写活。账上那一笔「已故」还在，这就要引擎配合改，算另一批活。

## 5. 测试

新开 `test/s22.js`，约 20 项：

- `splitAsk`：全角、半角、多段、嵌套、没有括号、只有括号，这几种都拆对
- 随心所欲档：提示词里行动只剩 `act`，要求逐条列出；【言出法随】里有括号那一段；静态前缀一字不变
- 括号里写「西陆」不触发过界；括号里的字不影响 `guessAttr` 挑的属性
- 另两档：拆都不拆，提示词跟改前逐字一样
- 面谈：随心所欲档的括号被标出来，另两档不动
- 回显：括号那一段用浅色显示
- （甲）括号要一万两，账上不多进；（乙）照给
- 回归 s1–s21、手机 54 项、跑量

## 6. 施工

一批做完，存档不升版本（不加字段）。

说「开工」，第 4 节那两项各选一个。
