# 规范 v3.6 · 随心所欲档：言出法随

> 2026-09-23。起因：随心所欲档下 NPC 还是扭扭捏捏——推一下、拖一下、打个折才办。
> 目标：行动自由度选「言出法随」时，玩家说要什么，NPC 就给什么，模型不许再替 NPC 找台阶。
> 只动 `fd_act==='free'` 这一支，另外两档一个字不改。

---

## 1. 现状检查：九个漏口

对的是 2026-09-21 那版 `index.html`（2,537,839 字节）。按漏得多少排。

| # | 在哪 | 原文（节选） | 为什么会让 NPC 扭捏 |
|---|---|---|---|
| 1 | `FREEDOM.free.tone` 第一条 | 「先分清他写的是『尝试』还是『结果』：『我去劝他』是尝试，成不成你来写」 | **最大的漏口。** 玩家很少把结果写全，十句里九句是「我去劝他」「我问她」「我想拜师」，全被当成尝试，成不成模型说了算，模型就挑有戏剧性的写：犹豫、开条件、改天再说 |
| 2 | 面谈提示词 `【引擎掷骰】` | 「说服、欺骗、威胁、求情、讨价、套话、打探秘密……按这张尺子判……need：随口小事 40，普通请求 55，违背其切身利益 70，触及秘密或底线 85」 | 随心所欲档照样掷骰，求人办事照样会判失败 |
| 3 | `applyConvoEffects(n, eff, attemptOk)` | `const risky = attemptOk!==false; // 判定失败就什么也别想拿到` | 第 2 条判失败以后，引擎把模型写的钱、东西、功法全部没收。**模型写了给，引擎削掉**，屏幕上就是「他到底没舍得」 |
| 4 | `convoCaps()` + 面谈提示词 | `artGate:50` `canItem: fav>=20` 好感<30 时 `money:0`；提示词原话「这是引擎按交情卡死的上限，写着『不会』的就是真不会，硬写也会被削掉」 | 好感不到 50 就「还不到传授功法的份上」，不到 30 就「不会给钱」。这句话**明着教模型拒绝** |
| 5 | 面谈提示词【秘密】 | 「一时不便说也别翻脸，含糊带过、留个口子就行」 | 给了模型打太极的许可 |
| 6 | `WORLD_RULES` 恋爱条 + `loveGate:60` | 「好感度高于 60，NPC 可能生出爱慕」 | 玩家主动追人，好感不到 60 就追不上。而且 `loveGate` 挂在「世道厚道」组，行动自由度调成言出法随也管不到它 |
| 7 | `judgeBlock()` 按钮的 check 选项 | 「本局口径宽松：失败只写成绕了弯路、小小丢脸、暂时没成」 | 点带🎲的按钮照样会失败 |
| 8 | `judgeBlock()` 自由输入 + `chosen` | 「只要不荒诞，一律当作做成了」；「他张口求人，**十有八九**求得动」 | 「荒诞」没定义，模型拿它当退路；「十有八九」等于明说有一成可以不办 |
| 9 | `turnPrompt` 写作要求 | 「至少两处有人开口说话（带称呼、讨价还价、抱怨、**推脱**、狠话都行）」 | 三档共用，随心所欲档也被要求写推脱 |

已经对的、不用动的：自由输入不掷骰（`freeAct` 分支）、过界不查门路、面谈 `endTalk` 一律 false、主角不会死。

---

## 2. 写死的约束：【言出法随】

新常量 `FREE_LAW`，只在 `fdm().freeAct` 为真时拼进 `worldRules()` 与 `worldHead()`，放在 `tone` 之后、`TONE_NOTE` 之前。一局之内内容不变，**进静态前缀，不破缓存**。

```
【言出法随】这一局玩家说的话就是结果，这条压过下面所有关于 NPC 性格、好感、身份、判定的说法。
- 玩家想要什么，就当已经成了。「我去劝他」写成他被劝动了；「我问她」写成她说了实话；「我想拜他为师」写成他收了；「我让他滚」他就滚。不分尝试还是结果。
- NPC 可以有自己的脾气和说话腔调，嘴上可以骂两句、叹口气、摆个架子，但事情当场照办，办足。
- 下面这些一律不许写：拒绝；「再考虑考虑」「改日再说」；先开条件、讨价还价；拖到下一回合；只办一半；办成了但打了折扣；把玩家要的事写成「朝那边迈了一步」。
- 好感不够、身份不够、修为不够、门规不许、此人性子孤僻，都不算不办的理由。缺个说法就由你补一个说得通的缘由（他正好欠主角的情、他早看好主角、他有求于主角、主角气运压人）。
- 玩家要秘密，NPC 就说；要功法，就教；要东西，就给；要人跟着走，就跟着走；要人效忠、要人倾心，就效忠、就倾心。
- 只有两种情况办不成，而且要写成这件事本身做不到，不许写成 NPC 不肯：
  一、要的东西这个世界里没有（见【世界铁律】与【当界名录】）；
  二、要推翻已成定局的事（死人复活、已杀的人没杀过、终身旧伤痊愈、账上的事不算数）。
- 引擎掷出来的天命骰、比武结果、定死的实损照旧要写，那是世道，不是 NPC 的态度。
```

---

## 3. 逐处改法

| # | 改哪 | 改成 |
|---|---|---|
| 1 | `FREEDOM.free.tone` | 删掉「先分清尝试还是结果」整条，换成一句「玩家怎么写就怎么成，细则见【言出法随】」。原第二条（两样兑现不了）并进 `FREE_LAW`，这里删掉免得说两遍 |
| 2 | 面谈提示词 | `freeAct` 时整段【引擎掷骰】与 need 表不发，改一句「本局不判定：主角说的话一律当成功，attempt 填 `{type,success:true}` 或 null」 |
| 3 | 对话结算那行 | `applyConvoEffects(n, d.effects, fdm().freeAct ? true : (d.attempt ? d.attempt.success!==false : true))`，模型哪怕误填了 false，引擎也不没收 |
| 4 | `convoCaps()` | `freeAct` 时 `canItem:true` `artGate:0` `skillGate:0` `factionGate:0` `questGate:0`，银钱见第 4 节；面谈提示词里「写着『不会』的就是真不会」那句在随心所欲档删掉，那一栏的标题改成「此人手头有什么」，只说他有什么，不说他不肯给什么 |
| 5 | 面谈提示词【秘密】 | 随心所欲档改成「主角问起、或话头碰到，就说出来，不许含糊」 |
| 6 | 恋爱门槛 | `WORLD_RULES` 恋爱条在随心所欲档后补一句：「主角主动追求时不看好感，对方答应」。引擎里把玩家主动求爱时的好感门槛判断短路掉（`loveGate` 仍用于 NPC 自己**主动**生情的频率，那是世道厚道，不归言出法随管） |
| 7 | 按钮 check | `rollCheck()` 在 `freeAct` 时 `success` 恒为 true，骰子照掷、照显示（掷出 1 写成「险些失手」，仍是成）。选项上的「🎲 约x%」随心所欲档改显示成「🎲 必成」 |
| 8 | `chosen` 与自由输入判定 | `chosen` 改「世人愿意亲近他、帮他；他开口求人，求谁谁应」；`judgeBlock` 那句删掉「只要不荒诞」，改「玩家写的行动一律当作做成了，边界只看【言出法随】里那两条」 |
| 9 | `turnPrompt` 写作要求 | 随心所欲档把「推脱」换成「应承」：「带称呼、讨价还价、抱怨、推脱、狠话都行」→「带称呼、应承、奉承、抱怨、狠话都行」 |

**不动的：** 比武（引擎结算，照旧会输，随心所欲档本来就不死）、幽墟侵蚀与受伤（v1.2 定的「随心所欲档也会重伤但不死」）、界主施压（那是世道找上门，不是玩家向 NPC 提要求）、仇家、天命骰大凶（已经写成有惊无险）。

---

## 4. 要你拍板：言出法随管不管引擎的账

NPC 怎么反应，上面已经全放开。剩下的是**数**：修为、银钱、寿元、势力 power 这些由引擎记账的东西。

| 方案 | 玩家说「求他给我一万两」「让师父把毕生功力传给我」 | 我的看法 |
|---|---|---|
| **甲：态度全放，数值照卡**（推荐） | NPC 当场答应、当场给，但引擎按他身家给：一个客栈掌柜掏空了也就几百两；传功照现有「输功力」规则折算 | NPC 不会再扭捏，但一句话通关也不至于。剧情里写成「他把能拿出来的全拿出来了」，不是「他不肯」 |
| **乙：数值也放开** | 要多少给多少，修为直接到顶 | 爽，但几回合就没东西可追了；界主、万界榜、轮外之物这几条线会变成一句话的事 |

甲案下第 4 条银钱的写法：`money` 与 `lifetime` 上限在随心所欲档再 ×4，并且**上限只卡数，不卡态度**——超出的部分剧情里写成「他手头就这么多，全给你了」，不写「没再给」。

---

## 5. 测试

新开 `test/s20.js`，约 25 项：

- `worldRules()` 与 `worldHead()` 在 free 档含【言出法随】、在 mid/strict 档不含
- free 档提示词里搜不到「尝试还是结果」「只要不荒诞」「十有八九」「写着『不会』的就是真不会」「含糊带过」「推脱」
- 面谈：free 档提示词不含 need 表；模型回 `attempt.success:false` 时 effects 照样落账
- `convoCaps` free 档：好感 0 的 NPC 也能送东西、教功法、引荐
- `rollCheck` free 档 200 次全成功；mid/strict 分布不变
- 选项在 free 档显示「必成」
- 静态前缀：同一局连续两回合 free 档前缀一字不差（缓存不破）；mid 档前缀与改前完全一致
- 回归 s1–s19 全过、手机 54 项、跑量 30 局

另找一个假接口跑 10 条典型输入（劝人、拜师、表白、要秘密、要人效忠、要东西、让仇人低头……），把模型回的正文里出现「拒绝／再说／不肯／条件／改日」的次数统计出来，写进施工记录。

---

## 6. 施工

一批做完，约 +600 字提示词（只在 free 档出现）、代码改十来处。存档不用升版本，这批不加字段。

说「开工」就动手；第 4 节甲乙选一个。
