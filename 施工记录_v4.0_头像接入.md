# 命运之轮 · 施工记录 v4.0「头像接入」（S5）

> 2026-09-26。对应《美术规范_v4.0_人物形象.md》第 5 节。Lynch 拍板：头目撞脸那几张先不重出，这批直接接。

## 入库验收

- `check_all.py`：捏人+特型 124/124、名人 115/115、头目 22/22，尺寸、底色、留白零不合格。
- 人眼看出的三处，记账不返工：
  - 头目 64px 小圆撞脸：别西卜／该隐／德治（堕）／宿傩一组；堕落大魔导／申公豹／崇德一组；妲己和玉藻前一组。
  - 东荒、樱洲中年男名人偏像：赵公明、真武、镇元子、庄子、闻仲、赖光、张天师。
  - x_elf 跟莱戈拉斯、瑟兰迪尔同一路，x_buddha 跟如来同一路。
- 张数比规范写的少：桶图去掉了 22 个 `_04` 备选位，两张图集实际是 124 + 137＝261 张，不是 279 张。

## 改了什么

| 件 | 内容 |
|---|---|
| `art/万界v4/build.py` | 出两张图集：`AV_*` 捏人桶图+特型，124 张，128px，WebP q58，169KB；`FAME_*` 名人+头目，137 张，112px，q56，162KB。产出 `parts/av4.js` |
| `art/万界v4/desc.txt` | 261 行「文件名\|一句相貌」，平均 33 字，由 `tools/draft_desc.py` 和 `tools/make_desc.py` 从三份出图清单抽出来。逐张对过图，有三处按实际出图手改：w2f_02 改成黑卷发南方女巫，s4m_04 不是剃发，特型 18 条全部手写 |
| `art/万界v4/splice.py` | 把 av4.js 塞回 index.html，并改引擎。先存底到 `归档/index_v3.8_v4头像前.html` |
| 引擎 | 新增 `BOSS_FACE`（22）、`FAME_FACE`（115）、`FAME_ALIAS`（51 个常用叫法，如观世音、齐天大圣、二郎神、贞德、信长）和 `nameFace()`，认名顺序：原名 → 别名 → `NPC_SAME` 另一边<br>`avSlotOf()` 改成：手选 → 名人／头目（主角除外）→ 钉死的桶图 → 特型 → 桶 → 墨影<br>`avCellStyle()` 能从两张图集取格子<br>`npcPortrait()` 改成画像描述跟着实际那张脸走，老存档里存的旧图描述不再进提示词；自己上传的图照旧用它自己的描述<br>`porOf()` 让名人和头目不再借特型立绘（脸会对不上），只有吞噬者照旧用它那张<br>旧 `BOSS_AV`（8 类共用脸）删掉 |
| 捏人页 | 不用改：按界×性别自动摆满，现在每边 13–16 张，不摆名人 |
| 测试 | 新增 `test/s23.js` 32 项；`s4` 四条、`s21` 两条旧断言按新图集改写 |

## 结果

- index.html：2.56MB → 2.81MB（+250KB）。
- 回归：s1–s23、phone 全过；run.js 跑 60 局 678 回合零问题，提示词最长 18457 字。
- s11「阿步治好旧伤」那条偶发失败，8 次里 1 次，重跑能过，跟头像无关，先记着。

## 以后换图

1. 新图放进 `avatars/`、`fame/`、`bosses/`，文件名不变。
2. 在 `desc.txt` 里改对应那一行。
3. 跑 `python3 build.py`，再跑 `python3 splice.py`。

注意 splice.py 是按 v3.8 的源码写的一次性替换，引擎部分再跑第二遍会因为找不到原文而断言失败。以后只换图的话，用它第 1 步那几行替换 `<script>const AV_CELL` 那一段就够了。
