# 施工记录 v3.8 · 括号里的推演要求

> 对应《规范 v3.8 括号要求》。第 4 节：①选**甲**（数值仍由引擎记账），②选**守住**（两条边界照旧）。
> 施工日：2026-09-23。`index.html` 2,552,129 → **2,554,874** 字节。
> 改前整页备份：`归档/index_v3.7_括号前.html`。
> 自测：新开 `test/s22.js`，36 项；s1–s21、手机 54 项、跑量 60 局 665 回合全部通过。
> 存档版本不变，不加字段。

---

## 一、做了什么

**拆括号**：新增 `splitAsk()`，认全角「（）」和半角「()」。括号里套括号的，按最外层拆；括号没合上的，后面的字全算要求。拆出两样：括号外的行动 `act`，括号里的要求 `asks`，要求一条一条分开。

**只在言出法随档拆**：依据是 `fdm().freeAct`，也就是行动自由度选「言出法随」。另外两档不拆，行动原样交给模型；提示词和改之前逐字比对过，一样。

**回合提示词**：【玩家本回合行动】只写括号外的那句，下面新加一栏【玩家对这一回合的推演要求（逐条照办）】，按 1、2、3 编号。只写了括号、没写行动时，行动那栏写「这一回合没写具体行动，只有下面的推演要求」。

**【言出法随】加了一段**（静态内容，进缓存前缀）：

- 括号里的要求逐条照办、写足，不许省略、打折、换成别的，也不许拿「为了剧情合理」当理由改掉。
- 这些要求优先于【写作要求】里的字数、段落、对话处数、衔接、「必须往前一步」。
- 只有三样不让：JSON 格式；两条办不成的边界（世上没有的东西、已成定局的事）；银钱、修为、寿元、日子由引擎记账。括号里要一万两、要直接突破、要跳三年，正文照写，playerChanges 里只填这一回合实实在在到手的数。

**引擎只看括号外面**：以下三处都改成只读括号外的行动。括号里写「西陆」不会再把人带走。

- 过界判断 `travelIntent`
- 挑属性 `guessAttr`
- 阿步暗号

**面谈**：「主角这一句」只留括号外的话，括号里的内容单独列成一栏，标明「主角的动作，或他对这一段的要求：动作照着写进场面，要求逐条照办」。

**回显**：章节开头「➤」那一行，括号那段字色淡一些、字号小一号。只在言出法随档这样显示。

## 二、没做 / 注意

- 存档阁「读档」时，章节开头的回显是从旧 HTML 恢复的。之前存下的章节不会补上浅色。
- 甲案没有加引擎代码：playerChanges 的入账一直由引擎按规矩算，这次只是在提示词里写明，别按括号里点名的数去填。
- 比武后续（`duelAftermathPrompt`）不拆括号。比武是从选项进的，玩家输入不会走到这里。

## 三、测试

`test/s22.js` 共 36 项：

- `splitAsk` 八种写法：全角、半角、多段、嵌套、没括号、只有括号、括号没合上、全角半角混用。
- 言出法随档：提示词各段写对了；静态前缀两次生成一字不差；只有括号时的写法；括号里写「西陆」不触发过界；真走一回合，人没被带走，要求单独列出；回显浅色；面谈拆对。
- 另两档：行动、面谈、引擎、回显全部原样，worldRules 与改前逐字一致。

回归时 s2 有一次在仇家那几项报错退出，单独重跑三次都是 76 项全过。那几项靠随机数，本来就偶尔不稳，跟这次改动无关。

还没推 GitHub。
