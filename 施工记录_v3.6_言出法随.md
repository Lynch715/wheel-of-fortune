# 施工记录 v3.6 · 言出法随

> 对应《规范 v3.6 言出法随》，第 4 节按**甲案**：NPC 的态度全放开，数值照卡。
> 施工日：2026-09-23。`index.html` 2,537,839 → **2,540,848** 字节。
> 改前的整页归档在 `归档/index_v3.5_言出法随前.html`。
> 自测：s1–s19 **720 项** + 新增 **s20 41 项** ＝ **761 项**，手机 54 项，跑量 60 局 652 回合 0 问题，全过。
> 存档版本不动，这批没加字段。

---

## 一、写死的那一段

新常量 `FREE_LAW`（【言出法随】），由 `freeLaw()` 在 `fdm().freeAct` 为真时拼进 `worldRules()` 和 `worldHead()`，位置在 tone 之后、`TONE_NOTE` 之前。一局之内一个字不变，跟着静态前缀进缓存。

要点：玩家要什么就算已经成了，不分尝试和结果；NPC 嘴上可以有脾气，事情要当场办足；拒绝、「再考虑」、开条件、拖到下回合、只办一半、打折扣，这些一律不许写；好感、身份、修为、门规都不算不办的理由；给钱给物以家底为限，家底不够就写「他把能拿的全拿出来了」；办不成的只有「世上没有」和「推翻定局」这两种，也要写成事情本身做不到，不许写成 NPC 不肯。

原 tone 里的「两样兑现不了」挪进了这一段，tone 那边只留一句指过来，免得同一件事说两遍。

## 二、九处漏口逐条

| # | 改了什么 |
|---|---|
| 1 | tone 删掉「先分清尝试还是结果」，改为「怎么写就怎么成」 |
| 2 | 面谈：随心所欲档不再发【引擎掷骰】和 need 表，改成「不判成败，attempt 填 null」 |
| 3 | 对话结算：模型就算误填 `success:false`，也先改成 true 再落账，气泡上不会出现「✗ 败」 |
| 4 | `convoCaps()`：随心所欲档把送物、教功法、指点手艺、引荐、托付几道门槛全开；银钱按交情 65 那一档 ×4 卡（这一档单次最多 600 两、累计 2400 两）。那一栏标题改成「此人手头有什么（这是他的家底，不是他肯不肯）」，「写着不会就是真不会」那句删掉 |
| 5 | 秘密：主角问起、或者话题碰到了，就照实说，不许含糊，不许打岔 |
| 6 | 恋爱：【言出法随】里写明「主角追求谁，对方就答应」。引擎里本来就没有卡主角求爱的代码，`loveGate` 只管 NPC 自己动心的频率，这个不动 |
| 7 | 按钮上的 🎲：随心所欲档骰子照掷，结果一律算成功，掷出 1 记作「险些失手」；选项上标「必成」，不再标百分比 |
| 8 | 「十有八九」改成「求谁谁应」，「只要不荒诞」删掉 |
| 9 | 写作要求里的「讨价还价、抱怨、推脱」，随心所欲档换成「应承、奉承、抱怨」 |

## 三、规范里没写、施工时顺手改的

**请人同行、请人入伙这两个按钮。** 这两处也在按好感、修为掷骰拒人，跟「要人跟着走，就跟着走」对不上。随心所欲档下：

- 同行：好感门槛、「修为远在你之上」、T4/T5 名人要好感 90，这三道不拦了，谈吐那一掷一律成。关系不在四类里（比如只是「朋友」）也能请，新加的 `followKindAsk()` 把这种人归成「同路」一类；**仇人、对头照旧请不动**。
- 入伙：好感 65 那道门不拦，谈吐那一掷一律成。

**照卡的：** 人数上限（同行 3 人、亲传 5 人）、不在本界、请人的钱、T1–T3 名人和一势力之首「走不开」。这些是位子和家底上的限制，跟 NPC 愿不愿意没关系，按甲案留着。

**没动的：** 比武、门派对战、招弟子、授业、幽墟的伤与侵蚀、界主施压都不在这批里。这些是世道的账，不是 NPC 对玩家的态度。`rollCheck()` 本身没改，只在按钮判定、同行、入伙这三个出口改了结果，所以门派战那些照旧会输。

## 四、提示词

东荒开局量的：

| | 改前 | 改后 |
|---|---|---|
| 随心所欲 · worldRules | 8,601 | 8,988（+387） |
| 随心所欲 · 回合提示词 | 17,048 | 17,431（+383） |
| 随心所欲 · 面谈提示词 | 7,730 | 7,737（+7，need 表拿掉以后抵消了） |
| 万界传奇 · worldRules | 7,960 | 7,960（一字不差，s20 钉着） |

## 五、测试

`test/s20.js` 41 项：随心所欲档提示词里有【言出法随】、搜不到那几句旧话、同一局连续两回合前缀一字不差；面谈不发骰子、秘密照说；好感 0、修为比主角高的人照样给钱、教功法、认师父、引荐；要的钱超过家底时给到上限，措辞不带「不肯」；请人同行不拦，仇人照拦；选项标「必成」；另两档照旧掷骰、照旧看好感，worldRules 跟改前逐字比对一致；最后真按一次自由输入走完一整回合。

`test/s2.js` 改了一条旧断言：「不得推翻已成定局」原来查的是 `FREEDOM.free.tone`，这句现在挪进了 `FREE_LAW`，断言改去查那边。

仍没推 GitHub，`git push origin main` 要你在 Mac 终端自己跑。
