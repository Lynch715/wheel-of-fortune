# 规范 v3.7 · 头像后期手动调整

> 2026-09-23。要求：主角和 NPC 的头像，开局之后还能自己换。
> 先看规范，确认再动工。第 5 节有一项要你拍板。

---

## 1. 现在是怎么定脸的

| 谁 | 怎么定 | 能不能改 |
|---|---|---|
| 主角 | 捏人页可以挑，不挑就「随天意」：按出身界 × 性别 × 年龄段从图集里抽 | 开局以后改不了 |
| NPC | 第一次露面时算一次：身份里带关键词的走特型（仙人、骑士、忍者……），其余按界 × 性别 × 外观年龄 + 名字哈希抽；算出来钉死在 `n.avatar` | 改不了 |
| 幽墟头目 | `BOSS_AV` 按名字认脸，**压过** `n.avatar` | 改不了 |

图集一共 90 格（64 张普通脸 + 26 张特型）。立绘 `POR_IMG` 按格子名挂，换了格子立绘也跟着换，这一点不用另做。

另外有一处要一起管：NPC 的「相貌」文字 `n.portrait` 是按格子描述写的，模型写这个人长什么样全靠它。**只换图、不换文字，正文就会跟头像对不上。**

## 2. 入口

不往主界面加按钮。

- **NPC**：人物卡的头像右下角加一枚小「换」字，点开换脸框。死了的人也能换（人物名录里还看得见他）。
- **主角**：左上角面板上的自己头像，点一下就是换脸框。
- 比武框、对话框里的头像不加入口，换了以后跟着变。

## 3. 换脸框

```
┌ 给「冷面客」换一张脸 ─────────────────┐
│ [东荒] [西陆] [樱洲] [轮枢] [特型]  ☐只看男 │
│ ┌──┬──┬──┬──┬──┬──┬──┬──┐            │
│ │  │  │  │  │  │  │  │  │  ← 格子    │
│ └──┴──┴──┴──┴──┴──┴──┴──┘            │
│ 描述：四十上下，面冷，左眉一道旧疤……      │
│ [随天意] [用这张]                         │
└──────────────────────────────────────┘
```

- 打开时停在**这个人所在界**那一页，性别过滤按他的性别预先勾上，当前那张框出来。
- 五个页签把 90 格全摆出来，**不限界、不限年龄段**：想让东荒人用西陆的脸也行，自己的局自己说了算。
- 点一格，底下出这一格的描述（`AV_DESC`），点「用这张」才生效。
- 「随天意」：清掉手选，回到自动算的那一张。
- 以后图集加格子（v4.0 那批名人头像、捏人头像），框里自动多出来，不用改代码。

## 4. 换了以后

- 写 `n.avatar = 新格子`，另记 `n.avatarPick = true`（手选过）。
- **手选压过一切自动规则**：`avSlotOf()` 先看 `avatarPick`，再看 `BOSS_AV`，再看已钉死的 `n.avatar`。幽墟头目也能换。
- **相貌文字跟着换**：`n.portrait = avDesc(新格子)`，这样模型下一回合起就按新脸写这个人。主角同理（`p.portrait`）。
- 起居注记一行「给某某换了一张脸」，方便以后翻的时候对得上。不进提示词、不花模型调用，模型只是从下一回合起看到新的相貌文字。
- 年龄不动。挑了一张老脸给二十岁的人，面板照样写二十岁，相貌文字照新脸写，这个矛盾由你自己挑的时候把握。框里每一格的描述会写年纪段，看得见。
- 「随天意」：删掉 `avatarPick`，按原规则重算一次，并把 `portrait` 也换回去。
- 存档不升版本：`avatarPick` 缺省就是没手选过，老存档照旧。

## 5. 要你拍板：能不能用自己的图

| | 甲：只从图集挑 | 乙：图集之外还能上传自己的图 |
|---|---|---|
| 做法 | 上面那一套 | 换脸框末尾加一格「＋ 用自己的图」，选一张本地图片，裁成正方形、压成 160×160 的 webp（一张约 8KB），存进浏览器的 IndexedDB，跟章节库放一起，**不塞进存档那一格**，免得把存档撑爆 |
| 相貌文字 | 跟着格子走 | 上传的图没有描述，框里多一栏让你自己写一句（「三十来岁，瘦，穿灰袍」），不写就沿用原来的相貌文字 |
| 立绘 | 格子有立绘就有 | 上传的只当头像，人物卡上不出立绘 |
| 导出存档 | 照旧 | 导出时把用到的那几张图一并打进存档文件，换台设备导入也还在 |
| 工作量 | 小 | 多一倍，主要在裁图、IndexedDB 和导出导入 |

我的建议是**乙**：图集那 90 张迟早不够用，你手里 GPT 出的图也多，能直接拿来当头像比每次都等下一批图集进 build 方便。

## 6. 测试

新开 `test/s21.js`，约 20 项：

- 人物卡、主角面板点得开换脸框，默认停在所在界、性别过滤正确、当前那张框着
- 选中后 `avatar`、`avatarPick`、`portrait` 三样都变，面板、名录、对话框、比武框的头像都换了
- 幽墟头目换了脸以后不再被 `BOSS_AV` 顶回去
- 「随天意」回到自动那一张，`portrait` 也回去
- 下一回合的提示词里，这个人的相貌是新脸的描述
- 老存档（没有 `avatarPick`）读进来脸不变
- 立绘跟着新格子走，新格子没立绘就不显示
- （乙）上传一张图：裁方、压到 160、进 IndexedDB、存档导出再导入还在；删掉那张图以后退回自动脸，不开天窗
- 手机 390 宽下换脸框不横向溢出，格子至少 44px 好点
- 回归 s1–s20、手机 54 项

## 7. 施工

一批做完，存档不升版本。选乙的话，上传那部分跟换脸框同批做，不拆两次。

说「开工」并选甲或乙。
