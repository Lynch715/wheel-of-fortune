# 命运之轮 v0.8 —— 主线化改造规范

> 给施工方的说明：改的是项目根目录的 `index.html`（v0.7.3，约 976 KB，其中 60% 是内嵌图片）。
> `工匠人生-试玩版.html` 是另一条路上的试作，**不采用、不参考其代码**，只借它一个思路（目标卡）。
> 参照物是 `参考-武侠江湖模拟器.html`：这一版要把主界面的手感拉回武侠版。
> 动工前先通读本文；每一节末尾有验收项。做完 A、B 两节先给 Lynch 看，再做 C。

---

## 0. 为什么改

Lynch 的原话：主界面选项太多（八九个），莫名其妙；他最想保住的是**每回合的自由输入和模型叙事**；整个项目本意只是把武侠版换个世界观，玩法不变。

对着两份代码看，病根在这：

| | 武侠版 | 命运之轮 v0.7 |
|---|---|---|
| 选项从哪来 | 模型写完叙事顺手给 3–4 条，贴着故事走 | `buildOptions()` 按阶级和季节拼出 6 条固定菜单 + 最多 8 条【大事】，模型的 3 条混在里面 |
| 玩家读完故事看到什么 | 这段故事的下一句 | 一张每月差不多的菜单 |
| 故事谁在推 | 模型（通过选项） | 引擎的随机事件（0.24/月，彼此无关） |
| 目标 | 宿命 + 江湖榜，常驻、可够 | `p.goal` 只是侧栏一行提示 |

所以要做三件事：**选项回到模型手里；菜单撤出主区；人生目标做成一条看得见的主线。** 其余系统一个不删，全部退到二级入口。

---

## A. 主界面：只剩故事、三条选项、输入框

### A1. 选项来源

- `renderOptions()` 只渲染 **3 条**：来自 `S.llmOptions`（模型本月给的）。没有 key、模型没应声、或模型给的不足 3 条时，用 `fallbackOptions()`（新写）补到 3 条。
- `fallbackOptions()`：从现有 `buildOptions()` 的非大事项里取前 3 条，优先取和当前主线阶段相关的 kind（见 C4 的 `arcStage().kinds`）。
- 删掉 `renderOptions()` 里现在那套「模型 3 条 + 常规 6 条去重取 6」的合并逻辑。
- 布局：单列，不再两栏 `optgrid`。手机端三条选项加输入框必须在一屏内，不需要滚动（v0.7.3 那个 `min-height`/`max-height` 逻辑相应简化）。
- 选项按钮保留字形牌记（`OPT_GLYPH`）和判定提示：如果 `kind` 会触发 `rollCheck`，在选项右侧显示「🎲 谋略 · 约 62%」（照武侠版 `optChance` 的做法；命运之轮 `resolveAction` 里各 kind 用的属性和难度已经写死，抽一个 `kindCheck(kind)` 返回 {attr, dc} 即可）。

### A2. 模型选项的要求（改 `turnPrompt()` 和 `initPrompt()` 里 options 那一条）

把「options 给 3 条本月之后可做的事，短句，贴合主角身份与眼下处境」改成：

```
- options 恰好 3 条，每条 8—18 字。必须承接本段记事的结尾：是这段故事里的人、地方、悬着的事的下一步，
  不是泛泛的去处（禁止"去集市看看""去教堂做弥撒""做工挣钱"这类每个月都能说的话）。
- 三条里至少 1 条朝着【人生目标·当前阶段】走（见下方目标块），标 kind 为该阶段给的 kind 之一。
- 三条彼此要有取舍：一条稳、一条冒险、一条走人情或走偏门。
- kind 只能取 work/study/church/social/court/trade/fight/rest/free。
```

在 prompt 里新增一个 `goalBlock()`（放在 `playerBlock()` 之后），内容见 C4。

### A3. 自由输入

不动。`inputVerdict()` 闸门保留。输入框 placeholder 改成随主线阶段变化的例子（例：阶段是「找引路人」时提示「如：去修道院求见副院长」），由 `arcStage().hint` 给。

### A4. 「去处」抽屉

- 输入栏左边加一个按钮「去处」（手机上是图标）。点开一个底部抽屉（复用现有弹窗样式，石梁抬头），里面分两组：
  - **常事**：`buildOptions()` 的非大事项，全部列出（做活／求学／教堂／走动／赶集／料理领地／当差／买卖／出征／休养），每条带一行小字说明这个月会发生什么。
  - **本季大事**：现有那组【大事】按钮，照旧一季一桩，`S.quarterUsed` 逻辑不变。
- 从抽屉里点任何一条，效果和以前在主区点一样（走 `doTurn(raw,kind,big)`）。
- 抽屉里的大事若正好是主线当前阶段推荐的那一桩（见 C4 的 `arcStage().bigKind`），置顶并描金。

### A5. 验收

- 任意一局、任意一月，主区选项恰好 3 条，全部能对上刚读完的那段记事。
- 没有 key 时也是 3 条，且不是「做工／求学／教堂」老三样重复出现——至少两条跟主线阶段沾边。
- 「去处」抽屉里能找到 v0.7 主区原来的每一条（含大事）。
- 390×844 手机屏：目标卡 + 正文 + 3 选项 + 输入框，无需滚动正文以外的区域。

---

## B. 目标卡：把人生目标放到正文顶上

### B1. 立志改到开局

- 现在是 16 岁和 20 岁生日弹 `askGoal()`。改成：**开场章节写完立刻弹**（`initPrompt` 那一轮结束后、第一次 `renderAll` 前）。20 岁那次保留，作为「改志」的唯一机会（弹窗文案写明「二十岁了，还想图这个吗」，可选「不改」）。
- 继承开局（子女接手）同样在接手那一刻弹。
- 立志弹窗每条目标下面加一行：这条路的第一步是什么（取 C 里每条主线第一阶段的名字），让玩家知道选了之后马上要干嘛。

### B2. 目标卡

- 位置：`#main` 顶部、`#story` 之上，一条常驻窄卡（石梁样式的一个薄版），手机上一行，桌面上两行。
- 内容：`目标名 · 第 n 步／共 N 步 · 阶段名 ｜ 还差：xxx`。「还差」用引擎算的具体数（复用并扩展 `goalHint()`：给每个阶段返回 1–2 条未满足条件，写成「主君好感 60（现 41）」这种带现值的形式）。
- 点开目标卡：一个小弹窗，列出这条主线的全部阶段，走过的打勾并附一句「你当时选了什么」（从 `S.arc.history` 取），当前阶段列出全部门槛和现值，未来阶段只给名字。
- 目标达成或失败（见 C2）后卡片变成结语，一直挂着。
- 侧栏「世相」页里原来的目标那一栏保留，改成只显示同一份数据（不要两套逻辑）。

### B3. 验收

- 新局第一次能行动之前，玩家已经立了志，目标卡已经在顶上。
- 目标卡上的「还差」每月都变，且和引擎数值一致（抽三局手工核对）。

---

## C. 主线：每条人生目标一条骨干事件链

### C1. 机制

- 新增常量 `ARCS`，键是 `GOALS[].k`，值是阶段数组。每个阶段：

```js
{ id:'noble_1', name:'找一个引路人',
  kinds:['social','court'],          // 模型给选项时该朝这几种 kind 走；fallback 也按这个挑
  bigKind:'court',                   // 抽屉里置顶的那桩大事（可空）
  hint:'如：去主君的厅里递一封信',   // 输入框 placeholder
  need: p=>[ ['主君或主教好感 ≥ 50', patronFav(p)>=50, patronFav(p)] ],   // [文字, 是否满足, 现值]
  beat: {                            // 门槛全满足的那个月触发的场景（可选）
    title:'引路人开口了',
    text: p=>`……`,                   // 引擎写的 2–3 句情境，交给模型扩写；没 key 时直接显示
    choices:[ {t:'答应', fx:p=>{...}, tag:'稳'}, {t:'讨价还价', check:{attr:'谋略',dc:55}, fx:(p,ok)=>{...}, tag:'险'}, ... ]
  }
}
```

- `S.arc = { k:目标名, stage:0, history:[{stage,choice,year,month}], failed:false }`。`migrate()` 给旧存档补这个字段：有 `p.goal` 的从 stage 0 起算（门槛自然会一路放行），没有的等 20 岁弹窗。
- 每月 `advanceMonth()` 末尾调 `arcTick()`：当前阶段 `need` 全满足 → 若有 `beat`，置 `S.pending={type:'主线', stage}`；没有 `beat` 直接 `stage++`。`handlePending()` 里 `主线` 排在随机事件之前、孩子的事之后。
- `beat` 用现有 `openAsk` 弹窗，样式与随机事件相同，但抬头标「主线 · 第 n 步」。选项的 `check` 走 `rollCheck`，结果和 `fx` 的数值变化进当月的 `judgeBlock`，这样下个月的记事由模型写出来，和普通行动一样。选完 `stage++`，写 `history`。
- **主线 beat 触发的当月不触发随机事件**；主线 beat 之后的 3 个月，随机事件概率从 0.24 降到 0.10（让故事有喘息，也免得把主线冲散）。
- 最后一个阶段完成 → `p.goal.done=true`（现有逻辑），目标卡变结语，`logLife`。

### C2. 主线会断

每条主线定义一个 `fail: p=>bool`。断了目标卡变成「此路已断」，20 岁前允许在目标卡上重立一次；20 岁后断了就只能这么活下去（这一世的总览和墓志铭要写这一笔）。例：嫁入高门——已嫁给低于或同于出身的人；位列教阶——已还俗；跻身贵族——沦为奴隶。

### C3. 七条主线的阶段（施工方按此写门槛与 beat 文本；文本口吻按 `worldRules()` 里的文风要求，大白话，不许排比不许升华）

**跻身贵族**（起点阶级不同，前面几步按 `ladder()` 的梯级套用；阶段按「每上一级」滚动，下面写的是每一级共用的四拍）
1. 找一个引路人 — 门槛：有一个高于自己一级以上的 NPC 好感 ≥ 50。beat：他开口要一件事（替他跑一趟／出一笔钱／替他隐瞒一桩事），三选一，选「隐瞒」记一条把柄在对方手里。
2. 投名状 — 门槛：`ladder().need` 里除「好感」外的条件满足一半以上。beat：机会来了但要押上东西（一次出征／一桩官司作证／一笔大钱），失败扣名望，成功引路人好感 +20。
3. 门槛 — 门槛：`ladder().ok`。beat：册封／入行会／受圣职当天有个对头出来搅（同行举报出身、旧仇翻账），三选一（拿钱平事／当众对质·谋略判定／引路人出面·耗他 30 好感）。
4. 新阶级的第一个月 — 无门槛，自动触发，只写 beat：旧阶级的亲友来求助，收还是不收（收：民心/好感 +，钱 −；不收：恶名 +5，家里人情意 −）。然后 `stage` 回到 1，`ladder()` 指向下一级；到贵族即成。

**富甲一方**（钱的门槛沿现有 `240*500`，分四道：1/10、1/4、1/2、全额）
1. 第一桶金 — 门槛：家财 ≥ 目标 1/10。beat：有人拉你合伙（现有身边人「拉你合伙」那条线可直接接进来）。
2. 一桩大买卖 — 门槛：≥ 1/4，且做过 ≥ 3 次 trade。beat：赶集时有批货要赌（瘟疫年囤粮／羊毛期货／替领主垫税），判定谋略，输了掉 1/3 现钱。
3. 树大招风 — 门槛：≥ 1/2。beat：三选一——领主要借钱、教会说你放印子钱要你捐、行会要你当行首（各接现有的诉讼／捐献／行会逻辑）。
4. 守成 — 门槛：全额。beat：立遗嘱（接现有 `askWill`），选完即成。

**位列教阶**（教士出身；非教士选此目标，第 0 步是「入教门」，门槛 = 现有 `laicize` 的反向，即受圣职）
1. 识字与拉丁文 — 门槛：拉丁文 ≥ 40。beat：抄经房里发现上司账目有假，报还是不报。
2. 上头有人 — 门槛：上司／主教好感 ≥ 65。beat：主教让你去办一件脏活（逐出一户欠什一税的农户／给贵族的私生子办洗礼），虔诚与好感二选一。
3. 大分裂里站队 — 门槛：年份 ≥ 1378，或教阶 ≥ 3。beat：接现有大分裂事件线的第一段，强制触发。
4. 座堂出缺 — 门槛：`ladder().ok` 且教阶 = 3。beat：两个候选人，你是其中一个，对手翻你的旧账（把柄有则用）。成即达。

**名垂青史**（名望 85）
1. 一件让人记住的事 — 门槛：名望 ≥ 35。beat：三条路——写一部书（要识字）／打一场仗／修一座堂（要钱），选定后这条主线后面的 beat 按路走。
2. 有人替你传 — 门槛：名望 ≥ 55。beat：行吟诗人／抄书人要把你的事写下来，给不给钱、让不让他添油加醋（加醋：名望 +10、恶名 +6）。
3. 编年史的一行 — 门槛：名望 ≥ 70。beat：修道院编年史家来问你的事，你讲的版本和别人讲的版本对不上（谋略判定，输了名望 −8、恶名 +4）。
4. 名望 85 即成。

**觅得真爱**
1. 遇见 — 门槛：有一个未婚异性 NPC 情意 ≥ 30。beat：对方家里另有安排，你要不要插手（送礼／找人说合／私下见面·判定）。
2. 门第 — 门槛：情意 ≥ 55。beat：接现有议亲逻辑，但对方是这个人；门第不合时引路人／嫁妆问题弹出来。
3. 婚后 — 门槛：已婚且配偶 = 那个人。beat：一次婚姻危机（钱／孩子没了／外人挑拨），三选一。
4. 情意 ≥ 80 即成。若对方死了或嫁了别人，主线断，20 岁前可重立。

**嫁入高门**（女性；男性选此目标时立志弹窗里不显示）
1. 出得了门 — 门槛：仪容 ≥ 45 或有一门场面技艺 ≥ 40。beat：一次筵席／教堂节庆，有人注意到你，家里让你去还是不去。
2. 高门里的那个人 — 门槛：有一个高出身两级以上的男性 NPC 好感 ≥ 45。beat：对方家里来打听你家底，父母想虚报嫁妆（谋略判定，输了名望 −10，这条线上的 NPC 好感 −20）。
3. 过门 — 门槛：`marriedUp >= 2`。beat：婆家的人给你难堪，接现有面谈系统强制一场面谈。成即达。
断：嫁给了不高于出身的人。

**平安到老**（六十岁；这条主线的 beat 是躲事）
1. 一处安身的地方 — 门槛：有房产或有主家（好感 ≥ 40）。beat：领主征召／行会摊派来了，出钱还是出人。
2. 瘟疫年 — 门槛：进入历史表里任一瘟疫年份。beat：走还是留（走：花钱、丢营生；留：死亡率按引擎算）。
3. 家里的事 — 门槛：年龄 ≥ 40。beat：孩子的前程或兄弟争产，接现有逻辑。
4. 老 — 门槛：年龄 ≥ 55。beat：立遗嘱（接 `askWill`）。到 60 即成。

### C4. `goalBlock()` —— 给模型的目标块

```
【人生目标】{目标名}——{目标一句话}。现在走到第 {n}/{N} 步：{阶段名}。
还差：{need 里未满足的条目，带现值}。
上一步你做的选择：{history 最后一条，没有则省略}。
本月的 options 里至少一条要朝这一步走，kind 取 {kinds}。
```

### C5. 验收

- 七条目标各跑一局到底（项目里没有留下以前的测试脚本，需要新写一个 `test/run-arcs.cjs`：用 node 加载 index.html 里的引擎部分，自动选行动跑到死或达成，断言下面几条）：每条主线的每个 beat 至少触发过一次，无报错，`history` 记录完整。
- 一局玩完，「这一世」总览页能按顺序列出主线的每一步和当时的选择——这就是 Lynch 说的「玩完能讲出一个故事」的最低要求。
- 随机事件不再在主线 beat 同月出现。

---

## D. 不动的东西

- 引擎权威、判定标尺、历史校验四道闸门、经济、婚育、继承、领地、集市、物品、遗嘱、对抗系统、面谈、身边人人生线、大分裂线、美术——**一行逻辑不改**，只是入口位置变了（进抽屉或不变）。
- 存档格式只加 `S.arc`，`migrate()` 兜底。
- 文风规则不动；新写的 beat 文本同样遵守：大白话、有具体东西、不排比、不升华、不写"你心里五味杂陈"。

---

## E. 施工顺序

1. **A**（主区瘦身 + 抽屉 + prompt 改选项规则）。做完给 Lynch 试，因为这一步单独就能感觉出来「像不像武侠版」。
2. **B**（开局立志 + 目标卡，先用现有 `goalHint()` 顶着）。
3. **C**（主线数据 + `arcTick` + 七条 beat 文本）。先做「跻身贵族」和「富甲一方」两条跑通机制，再补另外五条。
4. 回归：`test/run-arcs.cjs` 七条主线各一局，另外六个阶级 × 三档自由度各跑 20 条命确认没把经济和生死跑坏。
5. 版本记录写进 `规范与施工流程_v0.1.md` 末尾，格式照前几版。

每一步做完先给 Lynch 看，点头再下一步。

---

## F. 施工记录与规范增补（2026-09-13，A 节完工）

### F1. A 节已落地（`index.html`，v0.7.3 → v0.8-A）

- 主区 `renderOptions()` 只渲染 3 条，来源是 `S.llmOptions`；不足时 `fallbackOptions()` 用引擎的非大事项垫，优先挑和当前人生目标沾边的 kind，其余按 `S.turn` 转一格，免得每月同样三条。
- 选项右侧加判定成算：`kindCheck(kind)` 把 `resolveAction` 里各 kind 真用的属性式子和 DC 抽了出来，`kindChance()` 按 `rollCheck` 的 `val+(骰-10.5)*4>=dc` 数二十面的命中数。
- `mkOpt(o,withNote)` 统一造选项按钮，主区不带小字说明（带了手机上会折行），抽屉里带。
- 「去处」抽屉：`#goMask` + `openGo()`，常事全列（`buildOptions()` 去掉了 `slice(0,6)`）+ 本季大事；`arcBigKind()` 已留好置顶描金的接口。
- `turnPrompt`／`initPrompt` 的 options 规则按 A2 改写，新增 `goalBlock()`（现只有目标名 + `goalHint()` 的差额，C 节补「第 n 步／还差什么」）。
- `inputHint()` 按目标给 placeholder 例句，`arcStage()` 出来后自动改走 `arcStage().hint`。
- 顺手修的旧毛病：模型没给 options 时以前会留着上个月那三条。现在每回合开头 `S.llmOptions=null`，`applyLLM` 也改成无条件赋值。
- `#choices` 去掉了 48vh 限高和底部渐隐遮罩。390×844 实测：目标卡位置留白 + 正文 + 3 选项 + 输入框一屏放得下，页面不滚。

留给 C 节的接口（现在都是 `typeof arcStage==='function'` 的软探测，主线做出来即自动接上）：
`arcStageSafe()` / `arcKinds()` / `arcBigKind()` / `inputHint()` / `goalBlock()`。

### F2. 规范增补：必须接模型（新，A6）

Lynch 2026-09-13：「不接入 api 就别让玩。」

- 没有 `cfg.key` 时：启动不进投胎，直接开设置面板；`startNewGame()` 有闸门；设置面板在没 key 且没开局时点背景、点 × 都关不掉。
- `plainNarrative()` / `plainOpening()` 保留，但只作为开局后 key 丢失或模型半路挂掉的兜底，章内挂一条红 chip 提醒去补 key。
- 设置面板的副标题改成「每月的记事是模型写的。没填好 key 开不了局」。

### F3. 规范增补：连接自测（新，A7）

- 设置面板加「连通性」一栏：`#cfgTest` 按钮 + `#cfgTestMsg` 一行结果。
- `testConn()` 拿框里现填的三项真发一次 `max_tokens:16` 的请求，不动已存的 `cfg`。25 秒超时。
- 结果分档说人话：401「key 不对或者过期了」／404「模型名或接口地址不对」／402「账户没余额了」／429「被限流了」／抓不到网「多半是地址写错，或者这个接口不许网页直接调——跨域」。通了则报回模型名、回的那两个字、耗时，并提醒点「存下」。
- 「存下」时 key 为空不关窗，直接在这一行报错；填好了则关窗，若还没开局就直接弹投胎。

### F4. 规范增补：字数标准（新）

Lynch 2026-09-13：「太惜字如金了。」原来 300—500 字一个月，四十年下来没多少东西。

- 每月记事：**700—1000 字，分三到五段**。并且规定了这些字往哪儿花：至少两处有人开口说话（带称呼、讨价还价、抱怨、脏话）；至少一处写清手上的活和眼前的东西（价钱、器物、天气、气味、哪儿疼）；至少一处是主角独处时的盘算。明写「写长不是把一句话说三遍，是多写几件真发生的事」。
- 开场 `opening`：600—850 字、三到四段，结尾的钩子要具体到某个人／某笔钱／某件东西。
- 身世 `backstory`：300—420 字。
- 面谈回话：两到四句，引号外夹动作与语气。
- token 上限同步放宽：`callLLM` 默认 2200→3200，`llmTurn` 2400→4600，面谈 700→1000；`backstory` 截断 420→900，`appearance` 120→260。

### F5. 待 Lynch 拍板

1. 「求人教你认字」这类选项会标出「🎲 学识 · 约0%」——不识字的贫民学识够不着 DC 55，而且这引擎 `rollCheck` 掷 20 也不自动成功（武侠版是自动成功的）。但求学即便判定失败每月照涨 1—2 点，认字本来就是这么磨的。要不要给 `rollCheck` 加「掷 20 必成」，或者 0% 换个说法。
2. B 节没做之前，开局头几个月还没立志，垫底的三条没方向。B 节（开场写完立刻弹立志）会自然解决。

---

## G. 规范增补（2026-09-13 晚，A 节试玩后）

Lynch 试玩反馈：「现在的感觉好了很多。剧情推演也很不错。」随后提了两条。

### G1. 人物说的话要挑出来（照武侠版）

- `markSpeech()`：`esc()` 之后把 `「…」`、`“…”`、`"…"` 三种都包成 `<q>`。三条正则各限 160 字、不跨行，防止一个落单的引号把整段吞掉。
- 样式照 `参考-武侠江湖模拟器.html` 的 `.ntext q`：换色 + `quotes:"「" "」"` + 加粗。这里用 `var(--blue) #29456e`，在羊皮纸底上比武侠版那个金色清楚，也不跟界面的金线打架。
- 落地处：章节正文 `narrHtml()`、面谈气泡里对方那一侧 `renderConvo()`。
- `worldRules()` 的【文风】末尾加了一条【标点】：人物说的话一律用「」，一句一括，不许用引号、破折号、冒号引语，不许把说话和旁白混在一句里。——不加这条，模型给什么括号是看运气的。

### G2. 自由输入分档处理

Lynch 原话：「对玩家自己输入的行动要充分理解和支持。随心所欲难度下是全力配合推演，其他难度下则需要看命运骰子的点数。」

- `resolveAction` 的 free 分支重写：
  - `S.freedom==='free'` → `j.freeMode='comply'`，**不掷骰**，起居注写一行「随心所欲：这件事照你写的办，不掷骰」。
  - 其余两档 → `j.freeMode='roll'`，掷一次**命运骰子**，DC：传奇 55 / 写实 62，成败记进 `j.freeOk`。
- `guessAttr(text)`：按玩家写的字挑属性（`FREE_ATTR` 五条正则：武艺／谋略／学识／仪容／体质），挑不出来才退回最高属性。以前一律按最高属性掷，一个学者去跟人动手也凭学识，说不通。
- `freeBlock(j)` 插进 `turnPrompt` 的判定块之后：
  - comply：当他写的事确实发生了来写，整月围着它铺开；但世界的规矩一步不让（没有超自然、没有后世物、钱粮身份路程季节照卡、名录外的历史人物禁止）。他没写到的由模型补（谁在场、花了多少钱、旁人反应、留下什么后患）。身份根本够不着的事不驳回，改写成"他今天能够到的那一步"，并写清被什么挡住。
  - roll：骰子的结果就是结果，不许反转；没办成也必须往前推一步，不许把这个月写成什么都没发生。
- 注意：comply 档不掷骰 = 引擎数值不因这条行动而变，但模型回的 `npcUpdates` 照常生效，所以人情关系仍然会动。这是有意的——玩家改不了引擎的账，改得了人。

### G3. 顺手改的：掷 20 必成、掷 1 必败

`rollCheck` 以前纯比大小，掷出 20 也可能判失败（`crit` 标着、事却没办成），说不通；而且不识字的贫民去求学会标成「约 0%」，标出来就没人敢点。武侠版本来就是掷 20 必成。改成 `die===20 ? true : (die===1 ? false : val>=need)`，`kindChance()` 同步。结果：再难的事也有 5%，再稳的事也有 5% 翻车。**这条改了全局判定的标尺**，要是觉得手感变松，删掉这两行就回去了。
